<!-- ansible/templates/index.html.j2 -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CloudGuard Multi-Cloud Orchestration - {{ cloud_provider }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #fff;
        }
        
        .container {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
            border: 1px solid rgba(255, 255, 255, 0.18);
            max-width: 600px;
            width: 90%;
        }
        
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .logo {
            font-size: 3em;
            margin-bottom: 10px;
        }
        
        h1 {
            font-size: 1.8em;
            font-weight: 600;
            margin-bottom: 10px;
        }
        
        .provider-badge {
            display: inline-block;
            background: rgba(255, 255, 255, 0.2);
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.9em;
            margin-top: 10px;
        }
        
        .status {
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 20px 0;
        }
        
        .status-indicator {
            width: 12px;
            height: 12px;
            background: #4ade80;
            border-radius: 50%;
            margin-right: 10px;
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0% {
                box-shadow: 0 0 0 0 rgba(74, 222, 128, 0.7);
            }
            70% {
                box-shadow: 0 0 0 10px rgba(74, 222, 128, 0);
            }
            100% {
                box-shadow: 0 0 0 0 rgba(74, 222, 128, 0);
            }
        }
        
        .info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin: 30px 0;
        }
        
        .info-card {
            background: rgba(255, 255, 255, 0.1);
            padding: 15px;
            border-radius: 10px;
        }
        
        .info-label {
            font-size: 0.85em;
            opacity: 0.8;
            margin-bottom: 5px;
        }
        
        .info-value {
            font-size: 1.1em;
            font-weight: 600;
        }
        
        .endpoints {
            margin-top: 30px;
            padding: 20px;
            background: rgba(0, 0, 0, 0.2);
            border-radius: 10px;
        }
        
        .endpoint-title {
            font-size: 1em;
            margin-bottom: 15px;
            opacity: 0.9;
        }
        
        .endpoint-item {
            display: flex;
            justify-content: space-between;
            margin: 10px 0;
            padding: 10px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 5px;
            font-family: 'Courier New', monospace;
            font-size: 0.9em;
        }
        
        .endpoint-item:hover {
            background: rgba(255, 255, 255, 0.1);
        }
        
        .method {
            color: #4ade80;
            font-weight: bold;
        }
        
        .footer {
            text-align: center;
            margin-top: 30px;
            font-size: 0.85em;
            opacity: 0.7;
        }
        
        .architecture {
            margin: 30px 0;
            padding: 20px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 10px;
        }
        
        .architecture-title {
            font-size: 1.1em;
            margin-bottom: 15px;
            text-align: center;
        }
        
        .tech-stack {
            display: flex;
            justify-content: space-around;
            flex-wrap: wrap;
            gap: 10px;
            margin-top: 15px;
        }
        
        .tech-badge {
            background: rgba(255, 255, 255, 0.1);
            padding: 5px 12px;
            border-radius: 15px;
            font-size: 0.85em;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo">☁️</div>
            <h1>CloudGuard Platform</h1>
            <div class="provider-badge">{{ cloud_provider }} Infrastructure</div>
        </div>
        
        <div class="status">
            <div class="status-indicator"></div>
            <span>Instance Active</span>
        </div>
        
        <div class="info-grid">
            <div class="info-card">
                <div class="info-label">Instance Type</div>
                <div class="info-value">
                    {% if cloud_provider == 'AWS' %}t2.micro
                    {% elif cloud_provider == 'Azure' %}Standard_B1s
                    {% else %}e2-micro
                    {% endif %}
                </div>
            </div>
            <div class="info-card">
                <div class="info-label">Region</div>
                <div class="info-value">
                    {% if cloud_provider == 'AWS' %}eu-west-2
                    {% elif cloud_provider == 'Azure' %}UK South
                    {% else %}europe-west2
                    {% endif %}
                </div>
            </div>
            <div class="info-card">
                <div class="info-label">OS Family</div>
                <div class="info-value">{{ ansible_os_family }}</div>
            </div>
            <div class="info-card">
                <div class="info-label">Monitoring</div>
                <div class="info-value">Datadog EU</div>
            </div>
        </div>
        
        <div class="architecture">
            <div class="architecture-title">🗿 Architecture Stack</div>
            <div class="tech-stack">
                <span class="tech-badge">Terraform</span>
                <span class="tech-badge">Ansible</span>
                <span class="tech-badge">GitHub Actions</span>
                <span class="tech-badge">Datadog</span>
                <span class="tech-badge">Nginx</span>
            </div>
        </div>
        
        <div class="endpoints">
            <div class="endpoint-title">Available Endpoints</div>
            <div class="endpoint-item">
                <span><span class="method">GET</span> /</span>
                <span>This page</span>
            </div>
            <div class="endpoint-item">
                <span><span class="method">GET</span> /health</span>
                <span>Health Check API</span>
            </div>
            <div class="endpoint-item">
                <span><span class="method">GET</span> /nginx_status</span>
                <span>Nginx Metrics (Internal)</span>
            </div>
        </div>
        
        <div class="footer">
            CloudGuard - Multi-Cloud Orchestration Platform<br>
            Deployed via GitOps | Managed by Terraform & Ansible | Monitored by Datadog<br>
            Instance: {{ ansible_hostname }}
        </div>
    </div>
    
    <script>
        // Simple health check
        setInterval(() => {
            fetch('/health')
                .then(response => response.json())
                .then(data => {
                    console.log('Health Check:', data);
                })
                .catch(error => {
                    console.error('Health check failed:', error);
                });
        }, 30000); // Check every 30 seconds
    </script>
</body>
</html>